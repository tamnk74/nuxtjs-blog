import router from '@/constants/router';
import api from '@/constants/api';
import config from '@/constants/config';
import http from '@/constants/http_code';

export default {
  router,
  api,
  config,
  http,
}
